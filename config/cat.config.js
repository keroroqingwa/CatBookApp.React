/**
 * 项目配置
 */
const catconfig = {
  /**
   * 项目名称，页面title等使用
   */
  projectName: '喵喵看书 v3', // 喵喵看书后台
  /**
   * 用作项目某些配置（如 window.localStorage）key的前缀
   */
  projectPrefix: 'cat-book',
  /**
   * 数据接口的代理
   */
  //proxy: 'http://localhost:53518',
  proxy: 'https://v3.backstageApi.book.somethingwhat.com',
};

export default catconfig;
